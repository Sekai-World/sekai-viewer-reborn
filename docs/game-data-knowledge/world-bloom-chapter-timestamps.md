# World Bloom chapter timestamps

- World Bloom records use `chapterStartAt`, `chapterEndAt`, and `aggregateAt`; each may be an ISO string, Unix number, or null. The tools-site parser preserves these API field names (`apps/tools-site/src/lib/server/world-bloom.ts`).
