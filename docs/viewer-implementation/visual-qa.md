# Visual QA

Visual-regression snapshots must be approved or generated on ubuntu-24.04 Chromium through the manual **Visual baseline candidate** workflow, because macOS and CI rendering can differ. The CI artifact from PR #160 is the accepted source for the current-event baselines.
