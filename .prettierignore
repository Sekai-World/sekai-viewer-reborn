node_modules
.pnpm-store
.svelte-kit
dist
build
**/*.gen.ts

# pnpm rewrites the lockfile in its own YAML style on every install.
pnpm-lock.yaml

# SDK entry points regenerated by @hey-api/openapi-ts alongside the *.gen.ts files.
packages/sekai-*-sdk/src/index.ts
packages/sekai-*-sdk/src/client/index.ts

# Pinned vendor artifact (Live2D Cubism Core, SHA-256 recorded in
# docs/media-lab/api-contract-audit.md); it must stay byte-identical.
apps/media-lab-site/static/live2d/cubism-core/

# Helm templates contain Go template expressions, which Prettier's YAML parser
# cannot parse. Validate them with Helm instead.
deploy/helm/**/templates/**
